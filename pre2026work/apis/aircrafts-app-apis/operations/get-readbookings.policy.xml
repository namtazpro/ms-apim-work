<!--
    IMPORTANT:
    - Policy elements can appear only within the <inbound>, <outbound>, <backend> section elements.
    - To apply a policy to the incoming request (before it is forwarded to the backend service), place a corresponding policy element within the <inbound> section element.
    - To apply a policy to the outgoing response (before it is sent back to the caller), place a corresponding policy element within the <outbound> section element.
    - To add a policy, place the cursor at the desired insertion point and select a policy from the sidebar.
    - To remove a policy, delete the corresponding policy statement from the policy document.
    - Position the <base> element within a section element to inherit all policies from the corresponding section element in the enclosing scope.
    - Remove the <base> element to prevent inheriting policies from the corresponding section element in the enclosing scope.
    - Policies are applied in the order of their appearance, from the top down.
    - Comments within policy elements are not supported and may disappear. Place your comments between policy elements or at a higher level scope.
-->
<policies>
	<inbound>
		<base />
		<validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized" require-expiration-time="true" require-scheme="Bearer" require-signed-tokens="true">
			<openid-config url="https://login.microsoftonline.com/29ef4a4a-ba64-4e45-80db-4742c948b500/.well-known/openid-configuration" />
			<required-claims>
				<claim name="aud" match="all">
					<value>api://contoso1media/aircrafts-apis-publisher/Scope.Read.Bookings</value>
				</claim>
			</required-claims>
		</validate-jwt>
		<set-header id="apim-generated-policy" name="Ocp-Apim-Subscription-Key" exists-action="delete" />
		<set-header id="Authorization" name="Authorization" exists-action="delete" />
		<authentication-managed-identity resource="https://func-aircrafts-app.azurewebsites.net" />
	</inbound>
	<backend>
		<base />
	</backend>
	<outbound>
		<set-header name="record_count" exists-action="override">
			<value>@{ 
                var sBody = context.Response.Body.As<JObject>(preserveContent: true); 
                JArray jsonarray = (JArray)sBody["parts"];
                return jsonarray.Count().ToString();
            }</value>
		</set-header>
		<set-body template="liquid">
        {
        "PARTS" : [
        {% JSONArrayFor cty in body.parts %}
            {
                "CODE" : "{{cty.seq}}",
                "DESC" : "{{cty.description}}",
                "DEPTH" : "{{cty.length}}"
            }
        {% endJSONArrayFor %}
        ]
        }
        </set-body>
		<base />
	</outbound>
	<on-error>
		<base />
	</on-error>
</policies>
